interface:
  display_name: "Orchestrate Tickets"
  short_description: "Run approved tickets in fresh Codex tasks"
  default_prompt: "Use $orchestrate-tickets to execute the approved ticket plan in fresh Codex tasks."
policy:
  allow_implicit_invocation: true
