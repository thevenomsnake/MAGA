# AI 协作方法

本项目描述人和 AI 如何在长期项目中分配注意力、保存上下文并交接工作。这些术语用于比较不同项目并形成跨项目手册。

## Language

**注意力衰减（Attention Decay）**：
随着有效上下文增长、目标混杂或时间拉长，先前信息在后续推理与执行中被正确检索、权衡和持续遵守的概率下降。这是工程上的行为描述，不表示模型内部注意力权重必然随文本距离单调降低。
_Avoid_: 模型变笨、上下文消失

**注意力工作区（Attention Workspace）**：
一个会话中围绕单一内聚目标加载的最小必要上下文。
_Avoid_: 项目记忆、完整知识库

**持久上下文（Durable Context）**：
写入仓库或其他可版本化真源、可由新会话重新加载的事实、术语、决策、任务状态与证据。
_Avoid_: 聊天记忆、会话摘要

**协作角色（Collaboration Role）**：
具有明确职责、输入、输出和权限边界的长期工作位置；具体会话只是该角色可替换的承担者。
_Avoid_: 固定聊天、固定模型

**任务契约（Task Contract）**：
执行前固定的目标、行为边界、非目标、验收方式与依赖，是协调会话和执行会话之间的需求真源。
_Avoid_: 临时提示词、聊天转述

**交接记录（Handoff Record）**：
让另一个会话无需重放完整聊天即可继续工作的最小状态快照，包括已决定事项、当前事实、未完成项和精确引用。
_Avoid_: 全量聊天导出、工作日志

**协调会话（Coordination Session）**：
维护目标、任务边界、依赖和验收结论的会话，不默认拥有所有实现或发布权限。
_Avoid_: 万能主会话、长期聊天窗口

**执行会话（Execution Session）**：
在一个任务契约和职责边界内产出可验证结果的会话，完成后把结果写回持久上下文。
_Avoid_: 子聊天、临时助手

