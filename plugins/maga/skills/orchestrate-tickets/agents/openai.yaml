interface:
  display_name: "MAGA · Coordinate Delivery"
  short_description: "Coordinate approved work across focused Codex tasks"
  default_prompt: "Use $orchestrate-tickets to run the approved Ticket contracts in fresh Codex tasks."
policy:
  allow_implicit_invocation: true
