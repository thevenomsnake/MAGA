interface:
  display_name: "MAGA · Bar Tester"
  short_description: "Test real use, not only expected inputs"
  default_prompt: "Use $bar-tester to test how this product will actually be used, including the boundary an expected-input checklist could miss."
policy:
  allow_implicit_invocation: true
