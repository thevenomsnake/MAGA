interface:
  display_name: "MAGA · Make a Prototype"
  short_description: "Build a throwaway artifact to answer one question"
  default_prompt: "Use $prototype to build a throwaway artifact that answers this product question."
policy:
  allow_implicit_invocation: true
