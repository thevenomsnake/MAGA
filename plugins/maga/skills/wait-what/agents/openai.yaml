interface:
  display_name: "MAGA Wait What"
  short_description: "Re-pitch an explanation that did not land"
  default_prompt: "Use $wait-what to re-pitch the last explanation with the missing context."
policy:
  allow_implicit_invocation: true
