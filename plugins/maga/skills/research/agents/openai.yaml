interface:
  display_name: "MAGA Research"
  short_description: "Research product decisions from trusted sources"
  default_prompt: "Use $research to investigate this product question using trusted primary sources."
policy:
  allow_implicit_invocation: true
