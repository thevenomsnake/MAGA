interface:
  display_name: "MAGA Diagnosing Bugs"
  short_description: "Find root causes and lock fixes with evidence"
  default_prompt: "Use $diagnosing-bugs to reproduce this failure, find its root cause, and verify the fix."
policy:
  allow_implicit_invocation: true
