interface:
  display_name: "MAGA TDD"
  short_description: "Build behavior through focused red-green cycles"
  default_prompt: "Use $tdd to build this behavior through focused red-green cycles."
policy:
  allow_implicit_invocation: true
