interface:
  display_name: "MAGA Code Review"
  short_description: "Review a build against its standards and intent"
  default_prompt: "Use $code-review to review this build against the repository standards and intended result."
policy:
  allow_implicit_invocation: true
