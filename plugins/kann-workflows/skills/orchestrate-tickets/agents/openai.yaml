interface:
  display_name: "Orchestrate Tickets"
  short_description: "Run approved Tickets across fresh Codex tasks"
  default_prompt: "Use $orchestrate-tickets to run the approved Ticket contracts in fresh Codex tasks."
policy:
  allow_implicit_invocation: true
