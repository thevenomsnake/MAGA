interface:
  display_name: "Orchestrate Tickets"
  short_description: "Run approved missions across fresh Codex tasks"
  default_prompt: "Use $orchestrate-tickets to run the approved mission contracts in fresh Codex tasks."
policy:
  allow_implicit_invocation: true
